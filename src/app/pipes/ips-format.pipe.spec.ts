import { IpsFormatPipe } from './ips-format.pipe';

describe('IpsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IpsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
